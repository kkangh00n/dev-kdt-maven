<configuration>

  <conversionRule converterClass="org.springframework.boot.logging.logback.ColorConverter" />

  <!-- %-5 level -> 최대 5칸 왼쪽정렬, logger -> 최대 36자 (오른쪽에서부터 채움)  -->
  <property name = "LOG_PATTERN" value = "%clr(%d{HH:mm:ss.SSS}){yellow} [%thread] %clr(%-5level) %logger{36} -%kvp- %msg%n" />
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <!-- encoders are assigned the type
         ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
    <encoder>
      <pattern>${LOG_PATTERN}</pattern>
    </encoder>
  </appender>

  <appender name="FILE" class="ch.qos.logback.core.FileAppender">
    <file>logs/kdt_${bySecond}.log</file>
<!--    덮어쓰기-->
    <append>false</append>
    <encoder>
      <pattern>${LOG_PATTERN}</pattern>
    </encoder>
  </appender>

  <logger name="org.prgms.kdt" level="info">
    <appender-ref ref="FILE" />
  </logger>

  <root level="debug">
    <appender-ref ref="STDOUT" />
  </root>
</configuration>